<div class="p-8 space-y-4">
  <p class="text-sm text-gray-600 mb-4">Icons would go here (add icon library in future phase)</p>
  <div class="flex flex-wrap gap-4">
    <%= render Base::ButtonComponent.new(variant: :primary) do %>
      ✓ Save Changes
    <% end %>
    
    <%= render Base::ButtonComponent.new(variant: :danger) do %>
      × Delete
    <% end %>
  </div>
</div>
