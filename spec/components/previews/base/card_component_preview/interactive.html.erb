<div class="p-8 bg-gray-100 dark:bg-gray-800">
  <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
    <%= render Base::CardComponent.new(variant: :interactive, href: "#sarah") do |c| %>
      <% c.with_body do %>
        <div class="text-center">
          <div class="text-4xl mb-2">��</div>
          <h4 class="font-semibold text-gray-900 dark:text-white">Sarah</h4>
          <p class="text-sm text-gray-600 dark:text-gray-400">Fashion & Lifestyle</p>
          <div class="mt-2 text-xs text-green-600">Active</div>
        </div>
      <% end %>
    <% end %>

    <%= render Base::CardComponent.new(variant: :interactive, href: "#emma") do |c| %>
      <% c.with_body do %>
        <div class="text-center">
          <div class="text-4xl mb-2">👤</div>
          <h4 class="font-semibold text-gray-900 dark:text-white">Emma</h4>
          <p class="text-sm text-gray-600 dark:text-gray-400">Travel & Adventure</p>
          <div class="mt-2 text-xs text-green-600">Active</div>
        </div>
      <% end %>
    <% end %>

    <%= render Base::CardComponent.new(variant: :interactive, href: "#new") do |c| %>
      <% c.with_body do %>
        <div class="text-center text-gray-400 dark:text-gray-600">
          <div class="text-4xl mb-2">+</div>
          <h4 class="font-semibold">New Persona</h4>
          <p class="text-sm">Click to create</p>
        </div>
      <% end %>
    <% end %>
  </div>
</div>
