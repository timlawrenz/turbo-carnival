<!DOCTYPE html>
<html>
  <head>
    <title>New Pipeline Run</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-900 min-h-screen">
    <div class="container mx-auto px-4 py-8">
      <!-- Header -->
      <div class="flex justify-between items-center mb-8">
        <h1 class="text-3xl font-bold text-white">
          ➕ New Pipeline Run
        </h1>
        <%= link_to "← Back to Runs", runs_path, class: "px-4 py-2 bg-gray-700 hover:bg-gray-600 text-white rounded-lg transition-colors" %>
      </div>

      <% if flash[:alert] %>
        <div class="bg-red-600 text-white px-6 py-4 rounded-lg mb-6">
          <%= flash[:alert] %>
        </div>
      <% end %>

      <!-- Form -->
      <div class="bg-gray-800 rounded-lg shadow-xl p-8 max-w-2xl">
        <%= form_with model: @run, url: runs_path, local: true, class: "space-y-6" do |f| %>
          <!-- Pipeline Selection -->
          <div>
            <%= f.label :pipeline_id, "Pipeline", class: "block text-sm font-medium text-gray-300 mb-2" %>
            <%= f.select :pipeline_id, 
                options_for_select(@pipelines.map { |p| [p.name, p.id] }, @run.pipeline_id),
                { prompt: "Select a pipeline..." },
                class: "w-full px-4 py-2 bg-gray-700 border border-gray-600 rounded-lg text-white focus:ring-2 focus:ring-blue-500 focus:border-transparent" %>
            <% if @run.errors[:pipeline].any? %>
              <p class="mt-2 text-sm text-red-400"><%= @run.errors[:pipeline].first %></p>
            <% end %>
          </div>

          <!-- Run Name -->
          <div>
            <%= f.label :name, "Run Name", class: "block text-sm font-medium text-gray-300 mb-2" %>
            <%= f.text_field :name, 
                placeholder: "e.g., Gym Shoot - Morning Session",
                class: "w-full px-4 py-2 bg-gray-700 border border-gray-600 rounded-lg text-white placeholder-gray-500 focus:ring-2 focus:ring-blue-500 focus:border-transparent" %>
            <p class="mt-1 text-xs text-gray-400">Leave blank for auto-generated name</p>
          </div>

          <!-- Target Folder -->
          <div>
            <%= f.label :target_folder, "Target Folder", class: "block text-sm font-medium text-gray-300 mb-2" %>
            <%= f.text_field :target_folder, 
                placeholder: "/storage/runs/my-shoot",
                class: "w-full px-4 py-2 bg-gray-700 border border-gray-600 rounded-lg text-white placeholder-gray-500 focus:ring-2 focus:ring-blue-500 focus:border-transparent" %>
            <p class="mt-1 text-xs text-gray-400">Leave blank for auto-generated folder path</p>
          </div>

          <!-- Variables (JSON) -->
          <div>
            <%= f.label :variables, "Variables (JSON)", class: "block text-sm font-medium text-gray-300 mb-2" %>
            <%= f.text_area :variables, 
                value: @variables_json,
                placeholder: '{"prompt": "at the gym", "style": "cinematic"}',
                rows: 4,
                class: "w-full px-4 py-2 bg-gray-700 border border-gray-600 rounded-lg text-white placeholder-gray-500 font-mono text-sm focus:ring-2 focus:ring-blue-500 focus:border-transparent" %>
            <p class="mt-1 text-xs text-gray-400">Optional: Provide run-specific variables as JSON</p>
          </div>

          <!-- Submit Button -->
          <div class="flex gap-4 pt-4">
            <%= f.submit "Create Run", 
                class: "flex-1 px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white font-semibold rounded-lg transition-colors cursor-pointer" %>
            <%= link_to "Cancel", runs_path, 
                class: "flex-1 px-6 py-3 bg-gray-700 hover:bg-gray-600 text-white font-semibold rounded-lg transition-colors text-center" %>
          </div>
        <% end %>
      </div>
    </div>
  </body>
</html>
